#include <stdlib.h>

#include "cards_funcs.h"
//Initializing an empty list of cards//
Cards_List_init(Cards_List *cards);
//Freeing the allocated cards each round//
Cards_List_Free(Cards_List *cards){
    if (cards->length > 0){
        free(
    }
};